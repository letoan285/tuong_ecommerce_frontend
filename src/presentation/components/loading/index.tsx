import React from 'react';
import './loading.scss';

const Loading = () => {
    return (
        <div className="loading">
            <h5>Loading..</h5>
        </div>
    );
}

export default Loading;